# FNID Command Centre v2.0 - Production Dependencies
# Python 3.9+

# Web Framework
Flask>=3.0,<4.0
Flask-Login>=0.6,<1.0
Flask-WTF>=1.2,<2.0
Flask-Limiter>=3.7,<4.0
Flask-Talisman>=1.1,<2.0
Flask-CORS>=4.0,<5.0

# Security
bcrypt>=4.1,<5.0
cryptography>=42.0,<49.0

# Data & Export
openpyxl>=3.1,<4.0
xhtml2pdf>=0.2.16,<0.3

# Utilities
python-dotenv>=1.0,<2.0
gunicorn>=22.0,<23.0

# Monitoring
sentry-sdk[flask]>=2.0,<3.0
prometheus-flask-exporter>=0.23,<1.0

# Testing
pytest>=8.0
pytest-flask>=1.3
ruff>=0.4

# --------------------------------------------------------------------------
# The following are used by the src/fnid_portal v2 API variant (PostgreSQL).
# They are NOT required by the primary fnid_portal (SQLite) application but
# are kept here so that the v2 test suite (tests/test_auth.py,
# tests/test_registry.py, tests/test_services.py) can run.
# --------------------------------------------------------------------------
Flask-SQLAlchemy>=3.1,<4.0
Flask-Migrate>=4.0,<5.0
Flask-JWT-Extended>=4.6,<5.0
psycopg2-binary>=2.9,<3.0
SQLAlchemy>=2.0,<2.1
alembic>=1.13,<2.0
marshmallow>=3.21,<4.0
marshmallow-sqlalchemy>=1.0,<2.0
redis>=5.0,<6.0
celery>=5.4,<6.0
PyJWT>=2.8,<3.0
factory-boy>=3.3,<4.0
