# FNID Command Centre - Production Dependencies
# Python 3.11+

# Web framework
Flask>=3.0,<4.0
Flask-Login>=0.6,<1.0
Flask-WTF>=1.2,<2.0
Flask-Limiter>=3.7,<4.0
Flask-Talisman>=1.1,<2.0

# Security
bcrypt>=4.1,<5.0
cryptography>=42.0,<49.0

# Data & export
openpyxl>=3.1,<4.0
xhtml2pdf>=0.2.16,<0.3

# Utilities
python-dotenv>=1.0,<2.0

# Production server (Windows installer uses waitress; cross-platform pure-Python)
waitress>=3.0,<4.0

# Windows launcher tray icon
pystray>=0.19,<1.0
# Pillow is also pulled in by xhtml2pdf, but pin it for the launcher icon
Pillow>=10.0,<13.0

# Optional monitoring (only loaded if SENTRY_DSN is set)
sentry-sdk[flask]>=2.0,<3.0
prometheus-flask-exporter>=0.23,<1.0

# Testing & lint (dev only; not bundled in the installer)
pytest>=8.0
pytest-flask>=1.3
ruff>=0.4
